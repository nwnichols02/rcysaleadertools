import React from "react";
import { useNavigate } from "react-router";

export default function NewCalling(){
    // const navigate = useNavigate();
    // const loginRoute = () => {
    //     navigate('/')
    // }
    return(
        <div>
            <h1>NewCalling page</h1>
        </div>
    )
}